type: object
x-stoplight:
  id: m8ih62x0l9a5f
description: |-
  An application that implements operations for an API major version. It is responsible for secured access and management of an underlying data source.

  It is also referred to as a backend system because it lies behind the API proxy from the point of view of an consuming application.
required:
  - isBehindReverseProxy
  - createdAt
  - updatedAt
  - applicationAsset
  - proxy
properties:
  isBehindReverseProxy:
    type: boolean
    x-stoplight:
      id: 6bg7cmxgr495r
    default: false
    description: Whether or not the application server lies behind an SE regional reverse proxy. If it is hosted inside the SE VPN then it is accessed via a reverse proxy
  createdAt:
    $ref: ../models/CreatedAt.yaml
    description: The time this record was created
    x-stoplight:
      id: yk8rvudgyab8n
  updatedAt:
    $ref: ../models/UpdatedAt.yaml
    description: The time this record was last updated
    x-stoplight:
      id: 1gda68qeu4lia
  applicationAsset:
    $ref: ./Application Asset.yaml
    x-stoplight:
      id: tc4uiafv8ts5l
    description: Unique asset id and name in the SE asset system
  proxy:
    $ref: ./Proxy.yaml
    x-stoplight:
      id: 0s5cciyps7voy
    description: API Proxy
title: Proxy Provider
