type: object
x-stoplight:
  id: 90k3wl3068x8q
title: Path Mapping
description: 'When an API design document has a path that consumers use, and that path is different on the backend, the SEAPIM Platform needs to map the request before passing on to the backend. An instance of this is one path in one version being mapped for one backend.'
required:
  - id
  - specificationPath
  - createdAt
  - updatedAt
properties:
  id:
    $ref: ../models/Id.yaml
    x-stoplight:
      id: kl1eoc5iswmaf
  assetId:
    type: string
    x-stoplight:
      id: asimabk1zxqy7
    description: Application identifier in the SE technology asset management system.
  specificationPath:
    type: string
    x-stoplight:
      id: 4ewcwyny3hy80
    description: The path as documented in the API specification
  providerPath:
    type: string
    x-stoplight:
      id: u7sfx5qvbgg2c
    description: The path that exists in the provider system
  createdAt:
    $ref: ../models/CreatedAt.yaml
    description: The time this record was created
    x-stoplight:
      id: tcj2v9mu5cslz
  updatedAt:
    $ref: ../models/UpdatedAt.yaml
    description: The time this record was last updated
    x-stoplight:
      id: 88cu5fnt1li01
